import { NirValidatorDirective } from './nir-validator.directive';

describe('NirValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NirValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
